#ifdef DEBUG
#define _GLIBXX_DEBUG
#endif
#pragma GCC optimize("O3")
#include<bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    map<char,int> dict;
    int tmp,problem_cnt; // 一行目から読み取る数値
    string problem_str;
    vector<int,int> q_pos;
    cin >> tmp >> problem_cnt;
    cin >> problem_str;
    for (int i = 0; i < problem_cnt; i++)
    {
     //   q
    }
    
    return 0;
}
