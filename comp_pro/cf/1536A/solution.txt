最初に、aaに負の数が存在する場合、解決策は存在しないと主張します。 
Ppをaaの最小数として、qqを配列内の別の任意の数として示します（n≥2n≥2であるため、qqは常に存在します）。
明らかに、| q-p | = q-p 0 | q-p | = q-p0です。ただし、ppが負であるため、q-p qq-pqです。
そのため、出力配列にq-pq-pを追加すると、q-2p q-pq-2p q-pの差を持つペア（q-p、p）（q-p、p）が作成されます。以前と同じ問題があります。したがって、aaに負の数が存在する場合、適切な配列を作成することは不可能です。
このケースに対処した後、b = [0,1,2、...、100] b = [0,1,2、...、100]は、次のようなすべてのaaに対して有効なnice配列であると主張します。負の数は含まれていません。これが有効なnice配列であることを確認するのは簡単です。
また、この場合、aaのすべての要素は非負で明確であるため、要素を再配置してaaに追加し、bbを取得することは常に可能です。