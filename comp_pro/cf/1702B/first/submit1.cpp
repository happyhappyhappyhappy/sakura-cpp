#include<bits/stdc++.h>
using namespace std;
using ll=long long;
#define COUT(x) cout<<#x<< " = " <<(x)<< " (L" <<__LINE__<< ")" <<"\n" << flush

void initial(void){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int solver(string &input_str){
    int days =0;
    set<char> his_memory;
    
}

int main(void){
    initial();
    int testcase=0;
    cin >> testcase;
    while(testcase--){
        string words;
        cin >> words;
        cout << words << "\n" << flush;
    }
    return 0;
}
